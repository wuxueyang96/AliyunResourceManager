<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloudResourceManager"
        tools:targetApi="31">
        <activity
            android:name="life.wuxueyang.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CloudResourceManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="life.wuxueyang.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.CloudResourceManager" />
        
        <activity
            android:name="life.wuxueyang.CredentialSetupActivity"
            android:exported="false"
            android:label="Setup Credentials"
            android:theme="@style/Theme.CloudResourceManager" />
        
        <activity
            android:name="life.wuxueyang.DeviceAuthActivity"
            android:exported="false"
            android:label="Device Authentication"
            android:theme="@style/Theme.CloudResourceManager" />
    </application>

</manifest>